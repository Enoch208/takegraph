[project]
name = "takegraph"
version = "0.1.0"
description = "TAKEGRAPH — the self-healing build system for generative media"
requires-python = ">=3.12,<3.13"
dependencies = [
    "takegraph-domain",
    "takegraph-api",
    "takegraph-worker",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "hypothesis>=6.112",
    "ruff>=0.8",
    "mypy>=1.13",
]

[tool.uv.workspace]
members = ["packages/domain", "services/api", "services/worker"]

[tool.uv.sources]
takegraph-domain = { workspace = true }
takegraph-api = { workspace = true }
takegraph-worker = { workspace = true }

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC", "S", "T20", "RET", "SIM"]
# S101 assert: fine in tests. T201 print: fine in scripts.
ignore = ["S104"]

[tool.ruff.lint.per-file-ignores]
# S310: tests open presigned URLs this code generated, not user input.
"**/tests/**" = ["S101", "S310"]
# S310: _https() validates the scheme at runtime before every urlopen;
# ruff cannot see through the helper.
"scripts/**" = ["T201", "S310"]
# Alembic writes these. §23.4: generated files are not hand-edited, so line
# length is not enforced on them.
"infra/migrations/versions/*" = ["E501"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
plugins = ["pydantic.mypy"]
mypy_path = ["packages/domain", "services/api", "services/worker"]

[[tool.mypy.overrides]]
module = ["genblaze.*", "boto3.*", "botocore.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["packages/domain/tests", "services/api/tests", "services/worker/tests"]
filterwarnings = ["error::DeprecationWarning:takegraph_.*"]
