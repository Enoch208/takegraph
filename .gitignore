# Secrets — never commit. .env.example only.
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# Internal agent/AI working docs — local only, never pushed.
AGENTS.md
CLAUDE.md
GEMINI.md
.claude/
.cursor/
.cursorrules
.aider*
.windsurfrules
.github/copilot-instructions.md

# Internal specs and design-tool exports.
# TAKEGRAPH_Engineering_PRD.md is the internal implementation contract.
# t.json is a PAID aura.build template export (it carries the author's
# purchase URL and user id) — committing it would redistribute their product.
TAKEGRAPH_Engineering_PRD.md
t.json
*.aura.json
design-export*.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/
htmlcov/
.coverage
coverage.xml

# Node
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
.pnpm-store/

# Playwright
test-results/
playwright-report/
blob-report/

# Local media / scratch — generated evidence goes to B2, not git
tmp/
temp/
.tmp/
evidence/
*.mp4
*.mov
*.wav
*.mp3

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Next.js build artefacts already covered above; keep generated types out.
apps/web/next-env.d.ts
